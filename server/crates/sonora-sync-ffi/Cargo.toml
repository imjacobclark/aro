[package]
name = "sonora-sync-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lib]
crate-type = ["lib", "staticlib", "cdylib"]

[dependencies]
sonora-sync-protocol = { path = "../sonora-sync-protocol" }
